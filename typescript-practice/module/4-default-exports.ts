
export function add(x: number, y: number): number{
    return x+y;
}

export function subtract(x: number, y: number): number{
    return x-y;
}

// Create any one default function export
export default function multiply(x: number, y: number): number{
    return x*y;
}
