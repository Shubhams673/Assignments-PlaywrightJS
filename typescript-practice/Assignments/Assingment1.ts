// 1. Temperature of a city in degrees Celsius: 25.5
let temperature: number = 25.5;
console.log(`The temperature of the city is ${temperature}`);

// 2. Whether a customer has placed an order: true or false
let hasPlacedOrder: boolean = true;
console.log(`Whether a customer has placed an order ${hasPlacedOrder}`);

// 3. Person's phone number: "123-456-7890"
let personPhoneNumber: string = "123-456-7890";
console.log(`Person's Phone Number ${personPhoneNumber}`);

// 4. Amount of money in a customer's bank account: 1000.50
let bankAccountBalance: number = 1000.50;
console.log(`Amount of money in a customer's Bank Account ${bankAccountBalance}`);

// 5. Person's email address: "john.doe@example.com"
let personEmailAddress: string = "myofficialwork673@gmail.com";
console.log(`Person's Email Address${personEmailAddress}`);

// 6. Coordinates of a location (latitude, longitude): 37.7749, -122.4194
let coordinates ={
    latitude: 37.7749,
    longitude: -122.4194
}
console.log(`Coordinates of a location (latitude, longitude) ${coordinates.latitude}, ${coordinates.longitude}`); // It will print the coordinates of the location in the format "latitude, longitude", which is "37.7749, -122.4194" in this case. It is useful to represent geographical locations using latitude and longitude values for various purposes like mapping, navigation, etc.]`);   


// 7. Person's marital status: true or false
let isMarried: boolean = false;
console.log(`Person's Marital Status ${isMarried}`);

// 8. Person's occupation: "Software Engineer"
let personOccupation: string = "Software Engineer";
console.log(`Person's Occupation ${personOccupation}`);

// 9. Person's favourite colour: "Blue"
let favouriteColour: string = "Blue";
console.log(`Person's Favourite Colour ${favouriteColour}`);

// 10.Current year: 2023
let currentDate: Date = new Date();
let currentYear: number = currentDate.getFullYear();
console.log(`Current Year ${currentYear}`); // It will print the current year, which is 2024 in this case. It is useful to get the current year for various purposes like calculating age, determining leap years, etc.


// 11.Number of followers on a social media platform: 1,000,000
let numberOfFollowers: number = 1000000;
console.log(`Number of followers on a social media platform ${numberOfFollowers}`);

// 12.Rating of a movie: 7.5
let movieRating: number = 7.5;
console.log(`Rating of a movie ${movieRating}`);

// 13.Person's blood type: 'A'
let bloodType: string = 'A';
console.log(`Person's Blood Type ${bloodType}`);

// 14.Title of a book: "To Kill a Mockingbird"
let bookTitle: string = "To Kill a Mockingbird";
console.log(`Title of a book ${bookTitle}`);

// 15.Number of employees in a company: 500
let numberOfEmployees: number = 500;
console.log(`Number of employees in a company ${numberOfEmployees}`);

// 16.Time of an event: 2:30 PM
let time: Date = new Date();
let currenTime: (number | string) = time.getHours()+":"+time.getMinutes()+":"+time.getSeconds();
console.log(`Curren Time is -  ${currenTime}`); // It will print the current time in the format "HH:MM:SS", which is 12:00:00 in this case. It is useful to get the current time for various purposes like scheduling events, determining the time of day, etc.

// 17.Name of a country: "United States"
let countryName: string = "United States";
console.log(`Name of a country ${countryName}`);

// 18.Person's eye color: "Brown"
let eyeColor: string = "Brown";
console.log(`Person's Eye Color ${eyeColor}`);

// 19.Person's birthplace: "New York City"
let birthPlace: string = "New York City";
console.log(`Person's Birthplace ${birthPlace}`);

// 20. Distance between two cities: 200.5
let distanceBetweenTwoCities: number = 200.5;
console.log(`Distance between two cities ${distanceBetweenTwoCities} km`);